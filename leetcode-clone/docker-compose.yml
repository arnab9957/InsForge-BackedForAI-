version: '3.8'
services:
  execution-service:
    build: ./execution-service
    ports:
      - "3001:3001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PORT=3001
    network_mode: bridge
